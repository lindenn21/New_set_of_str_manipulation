# Prog01. rstrip() remove the space characters at the end of the string. Create a program that do the same functionality without using rstrip() function.
# Ask user to input something with spaces at the end
# specify the whitespace to be at the end by using len(text) - 1
# remove whitespace
# print

text = input("Enter: ")
